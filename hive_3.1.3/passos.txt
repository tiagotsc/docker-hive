create table teste123aa (column_a string, integer_first int);

select * from teste123aa;

insert into novo.teste123aa(column_a,integer_first) values('aaaa',1);


# Constrói a imagem
docker build hive -t hive:3.1.3

# Criar container
docker run -dit --net hadoop_dl_net --hostname hive --name hive -p 10002:10002 -p 10000:10000 --privileged hive:3.1.3 /usr/sbin/init

# Entre no container usando o usuário hduser
docker exec -u hduser -it hive /bin/bash


docker container rm -f hive
docker image rm hive:3.1.3

# Tez
export TEZ_HOME=/home/hduser/tez


################## CentOS 7 - MYSQL INSTALAÇÃO ######################
# Baixa pacotes de configuração do MySql
# Veja sempre no link abaixo a versão mais atual e substitua esse link no passo a passo
# https://dev.mysql.com/downloads/repo/yum/
# Baixe o Red Hat Enterprise Linux 7 / Oracle Linux 7
curl -sSLO https://dev.mysql.com/get/mysql80-community-release-el7-7.noarch.rpm


# Verifique o md5 e compare com o do site para ter certeza que os pacotes serão baixados correntamente
md5sum mysql80-community-release-el7-7.noarch.rpm

# Adicione o repositório MySql
sudo rpm -Uvh mysql80-community-release-el7-7.noarch.rpm

# Instale o MySql
sudo yum install mysql-server

# Inicie o serviço
sudo systemctl start mysqld

# Verifique o status o serviço
sudo systemctl status mysqld

# Pegue a senha root temporário do MySql
sudo grep 'temporary password' /var/log/mysqld.log

ijhzqniat9:W

Será pergun
primeira pergunta = n